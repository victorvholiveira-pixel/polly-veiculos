import { PagePlaceholder } from '@/components/PagePlaceholder'

export function StockPage() {
  return (
    <PagePlaceholder
      title="Estoque"
      description="A lista de veículos disponíveis vai aparecer aqui depois da migração e validação do estoque inicial (ver GO_LIVE_CHECKLIST.md)."
    />
  )
}
