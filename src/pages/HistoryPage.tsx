import { PagePlaceholder } from '@/components/PagePlaceholder'

export function HistoryPage() {
  return (
    <PagePlaceholder
      title="Histórico"
      description="As vendas migradas da planilha vão aparecer aqui, com filtro por mês, ano, veículo, placa e cliente."
    />
  )
}
